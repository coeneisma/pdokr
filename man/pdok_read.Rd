% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdok_read.R
\name{pdok_read}
\alias{pdok_read}
\title{Read a PDOK layer as an sf object}
\usage{
pdok_read(
  dataset,
  layer,
  bbox = NULL,
  filter_by = NULL,
  predicate = "intersects",
  datetime = NULL,
  crs = NULL,
  max_features = NULL
)
}
\arguments{
\item{dataset}{A dataset id from \code{\link[=pdok_list_datasets]{pdok_list_datasets()}} (e.g.
\code{"cbs/gebiedsindelingen"}), or a raw OGC API or WFS base URL.}

\item{layer}{A layer id from \code{\link[=pdok_list_layers]{pdok_list_layers()}}.}

\item{bbox}{Optional server-side bounding-box pre-filter: a numeric vector
\code{c(xmin, ymin, xmax, ymax)} (assumed CRS84) or an \code{sf}/\code{sfc}/\code{bbox} object
whose extent is used.}

\item{filter_by}{Optional \code{sf}/\code{sfc} geometry to filter the result by. Its
bounding box is used as a cheap server-side pre-filter, and the result is
then filtered exactly with \code{\link[=pdok_filter_by]{pdok_filter_by()}}. This is the one-call form of
the load-then-filter workflow. It is usually a polygon (e.g. a
municipality), but a point works too: filtering an area layer by a point
returns the feature that contains it (for example the municipality an
address falls in).}

\item{predicate}{Spatial predicate for \code{filter_by}, passed to
\code{\link[=pdok_filter_by]{pdok_filter_by()}} (default \code{"intersects"}).}

\item{datetime}{Optional temporal filter: a single year (e.g. \code{2026}, mapped
to a mid-year instant), an OGC datetime string, or an interval such as
\code{"2020-01-01/2025-12-31"}.}

\item{crs}{Optional output CRS as an EPSG code (e.g. \code{28992} for RD New).
\code{NULL} keeps the source CRS.}

\item{max_features}{Optional cap on the number of features returned.}
}
\value{
An \link[sf:st_sf]{sf} object with one row per feature, the layer's
attribute columns, and a geometry column. A zero-row \code{sf} is returned (with
a warning) when nothing matches.
}
\description{
Loads a layer from PDOK as a simple feature collection. The OGC API Features
service is used when available, with a transparent fallback to the Web
Feature Service ('WFS'). Pagination is handled automatically.
}
\details{
By default the data is returned in the coordinate reference system the
service provides (lon/lat, CRS84, for the OGC path). Set \code{crs} to receive the
data in another CRS; the transformation is done client-side with
\code{\link[sf:st_transform]{sf::st_transform()}}.
}
\examples{
\donttest{
# A whole layer: the Dutch national parks
parks <- pdok_read("rvo/nationale-parken-geharmoniseerd", "protectedsite")

# Municipalities for 2024, in RD New (EPSG:28992)
pdok_read(
  "cbs/gebiedsindelingen", "gemeente_gegeneraliseerd",
  datetime = 2024, crs = 28992, max_features = 5
)

# One-call area filter: national parks within the province of Utrecht
provinces <- pdok_read(
  "cbs/gebiedsindelingen", "provincie_gegeneraliseerd", datetime = 2024
)
utrecht <- provinces[provinces$statnaam == "Utrecht", ]
parks_utrecht <- pdok_read(
  "rvo/nationale-parken-geharmoniseerd", "protectedsite",
  filter_by = utrecht
)
}
}
\seealso{
\code{\link[=pdok_list_layers]{pdok_list_layers()}} to find layer ids.
}
