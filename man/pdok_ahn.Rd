% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ahn.R
\name{pdok_ahn}
\alias{pdok_ahn}
\title{Read AHN elevation for an area as a raster}
\usage{
pdok_ahn(area, model = c("dtm", "dsm"), crs = NULL)
}
\arguments{
\item{area}{An \code{sf}, \code{sfc}, or \code{bbox} object; its bounding box defines the
area to fetch. Any CRS is accepted and converted to RD New for the request.}

\item{model}{Which elevation model: \code{"dtm"} (digital terrain model — bare
ground) or \code{"dsm"} (digital surface model — including buildings and
vegetation). The difference \code{dsm - dtm} gives object heights.}

\item{crs}{Optional output CRS as an EPSG code to reproject to. \code{NULL} (the
default) keeps the native RD New (EPSG:28992).}
}
\value{
A \code{terra} \link[terra:rast]{SpatRaster} of heights in metres (NAP).
}
\description{
Downloads the 'Actueel Hoogtebestand Nederland' (AHN) height data for the
extent of \code{area} and returns it as a \code{terra} \code{SpatRaster}. Unlike pdokr's
vector functions, which return \code{sf}, this returns \strong{raster} data: a grid of
height values (metres relative to NAP) at 0.5 m resolution.
}
\details{
AHN is served through a classic OGC \strong{Web Coverage Service} (WCS), not an OGC
API. The bounding box requests a subset; the service limits the request size,
so areas larger than about 2 x 2 km (at 0.5 m) are rejected — read a smaller
area. Requires the \code{terra} package.
}
\examples{
\donttest{
# Terrain around the Utrecht Dom, and the surface (incl. buildings)
centre <- sf::st_buffer(pdok_geocode("Domplein, Utrecht", crs = 28992), 300)
dtm <- pdok_ahn(centre, "dtm")
dsm <- pdok_ahn(centre, "dsm")
object_height <- dsm - dtm
}
}
\seealso{
\code{\link[=pdok_read]{pdok_read()}} for vector data as \code{sf}.
}
