% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basemap.R
\name{pdok_basemap}
\alias{pdok_basemap}
\title{A PDOK basemap to use as a map background}
\usage{
pdok_basemap(style = "standaard", format = c("raster", "vector"))
}
\arguments{
\item{style}{The basemap style. For \code{format = "raster"}: one of
\code{"standaard"}, \code{"grijs"}, \code{"pastel"}, \code{"water"}, or \code{"luchtfoto"} (aerial
imagery). For \code{format = "vector"}: one of \code{"standaard"}, \code{"zonder_labels"},
\code{"luchtfoto"}, or \code{"darkmode"}.}

\item{format}{\code{"raster"} (the default) returns a WMTS tile-URL template
(\code{{z}/{x}/{y}}, in Web Mercator / EPSG:3857) that works with \code{tmap},
\code{leaflet} and \code{maplibre}/\code{mapgl}. \code{"vector"} returns a Mapbox GL style URL
for \code{maplibre}/\code{mapgl}.}
}
\value{
A single string: a raster tile-URL template, or a vector style URL.
}
\description{
Returns the URL of the official PDOK 'BRT Achtergrondkaart' (or aerial
imagery) for use as the background of a map. Nothing is downloaded — the
function only constructs the URL, so it works offline and instantly. Hand the
result to any mapping package: \code{tmap} and \code{leaflet} take the raster tile URL,
\code{maplibre}/\code{mapgl} take the vector style URL.
}
\section{Attribution}{

The map data is © Kadaster / PDOK. Show this attribution on any map that uses
the basemap.
}

\examples{
# Raster tile URL (tmap / leaflet)
pdok_basemap()
pdok_basemap("grijs")

# Vector style URL (maplibre / mapgl)
pdok_basemap("standaard", format = "vector")
}
