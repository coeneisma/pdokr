% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locatieserver.R
\name{pdok_reverse_geocode}
\alias{pdok_reverse_geocode}
\title{Reverse geocode coordinates to the nearest address with the PDOK Locatieserver}
\usage{
pdok_reverse_geocode(points, type = NULL, crs = NULL, limit = 1)
}
\arguments{
\item{points}{An \link[sf:st_sf]{sf} or \code{sfc} object of one or more points, in any
CRS. The coordinates are transformed to lon/lat internally for the query.}

\item{type}{Optional result type to restrict to (see \code{\link[=pdok_geocode]{pdok_geocode()}} for the
list); \code{NULL} (the default) returns the nearest match of any type.}

\item{crs}{Optional output CRS as an EPSG code (e.g. \code{28992}). \code{NULL} keeps
the source CRS (CRS84, lon/lat).}

\item{limit}{Maximum number of results to return per point (default 1, the
single nearest match).}
}
\value{
An \link[sf:st_sf]{sf} object with one row per match and a \code{point_id}
column giving the row index of the input point each match came from. The
\code{afstand} column holds the distance in meters; all other non-geometry
fields the service returns are kept too. Points that match nothing are
dropped (with a warning); a zero-row \code{sf} is returned when nothing matches
at all.
}
\description{
Finds the address, road or place nearest to each point, through the 'PDOK'
Locatieserver reverse service — the inverse of \code{\link[=pdok_geocode]{pdok_geocode()}}. Give it \code{sf}
points (in any CRS); it returns the nearest match(es) as an \code{sf}, including an
\code{afstand} column with the distance in meters.
}
\examples{
\donttest{
# A point in the center of Utrecht: the nearest address
pt <- sf::st_sfc(sf::st_point(c(5.121, 52.090)), crs = 4326)
pdok_reverse_geocode(pt)
}
}
\seealso{
\code{\link[=pdok_geocode]{pdok_geocode()}} for the forward lookup (address to coordinates).
}
